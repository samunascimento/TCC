
public static void grupo2{String repositorio}{

//Lista as versões
  show (repositorio)
 //if merge
    if(merge(null,repositorio))
   //listar país
     show(repositorio).filhos
     //if conflitos
         if(conflitos)
       // listar conflitos
           

}
